<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: 2026 SubLang International <https://sublang.ai> -->

# Compilation performance: 2026-09-12 progress

The five-minute target is **not achieved**. A [cold minimal workflow now passes every check in its original run](</private/tmp/slc-performance-evidence/compile-sxeXw2/summary.json>): **371.27 seconds to generate and 373.80 seconds including validation**. The main cost remains five serial LLM phases, especially FSM generation and linking. The earlier 407.14-second workflow retains its original failed summary and separate corrected runtime replay. The latest Opus trial finishes generation in 174.17 seconds but fails strict entry validation, so its shorter duration is not an accepted result.

**The first accepted matched speed improvement is in quoted-relay linking:** the [ordinary baseline](</private/tmp/slc-performance-evidence/compile-oNiH7r/summary.json>) takes 213.00 seconds and the [phase-owned helper](</private/tmp/slc-performance-evidence/compile-YQHwJk/summary.json>) 78.68 seconds, a **63.1% observed reduction**. Both pass strict and linked checks plus separate real-Git acceptance; calls fall from two to one and tools from 17 to nine. Retain this flat quoted-relay technique. One matched pair establishes the observed result, not a general latency guarantee or the cold five-minute target.

- **Generation dominates.** In that [valid-artifact run](</private/tmp/slc-performance-evidence/compile-aZda4G/summary.json>), FSM generation takes 179.79 seconds and linking 118.59 seconds: together, 298.38 of the 407.14 seconds. Improving small host operations alone cannot meet the target.
- **Late failures waste downstream work.** An invalid baseline FSM consumes another 158.22 seconds in linking before rejection; the [early check](</private/tmp/slc-gpt6-engine-audit/early-gate-evidence.json>) detects the same defect in 46.36 ms. Malformed GEARS similarly triggers three doomed consumer calls. Producer checks address this wasted work; accepted net speed evidence remains necessary.
- **Contract and tool failures add avoidable work.** Some linker refusals are [false host-boundary audits](</private/tmp/slc-gpt6-engine-audit/evidence.json>); another exposes a [real contradictory mapper clause](</private/tmp/slc-deferred-contract-audit/evidence.json>). The distinction matters: correcting guidance, a stale contract, a checker or an authored artifact requires different changes. A global-TypeScript retry adds another observed detour, with savings still unmeasured.
- **The ordinary CLI uses a different setup.** The [installed-command audit](</private/tmp/slc-global-runtime-audit/evidence.json>) finds global SLC 0.7.0 and nested Playbook 12.2.0; the user's Playbook directory selects `claude-opus-4-8` at high effort. Benchmarks use explicit newer compiler/dependency and model settings. No measured speed ratio connects those environments.

Combined first-event waiting accounts for 31.48 seconds, or 9.5%, of the original Opus baseline. It includes SDK/process startup and provider/network waiting, not isolated network latency or an additional cost to add to call duration.

The minimal-workflow experiments below use the same 292-byte source, cold output directories, low effort, optimization and no independent Reviewer. Later rows separately identify the broader demo, fixed-FSM linking and rejected settings. Work continues under [IR-027](../specs/intents/027-measured-compilation-performance.md) and the evidence policy in [DR-031](../specs/decisions/031-measured-compilation-performance.md).

The [sanitized measurement record](performance/2026-09-12.json) contains source, definition, lockfile and available compiler-runtime hashes, per-call accounting, reconstructed commands, validation flags, and original evidence paths. Raw prompts, responses, and diagnostic prose remain outside this repository.

| Settled experiment | Compile | FSM / link | Calls / tools | Accepted result |
| --- | ---: | ---: | ---: | --- |
| [Opus 5 baseline](</private/tmp/slc-performance-evidence/compile-iS1Lmh/summary.json>) | 330.03 s | 89.20 / 158.22 s | 5 / 15 | Failed link prompt fidelity; separate Git probe also found source drift |
| [Opus 5 split definition](</private/tmp/slc-performance-evidence/compile-umeUCL/summary.json>) | 356.24 s | 81.27 / 194.86 s | 5 / 22 | Failed linked-module role metadata; experiment rejected |
| [GPT-6, old SDK](</private/tmp/slc-performance-evidence/compile-IXAbh6/summary.json>) | 54.84 s | — | 1 / 0 | Incompatible Codex runtime; no compilation throughput result |
| [GPT-6, SDK 0.153.4](</private/tmp/slc-performance-evidence/compile-EXAwtt/summary.json>) | 556.18 s | 255.21 / 127.10 s | 5 / 22 | Linker incorrectly audited host validation at the shared-factory boundary |
| [Opus 5, early gate and repairs](</private/tmp/slc-performance-evidence/compile-k6mg7y/summary.json>) | 485.50 s | 115.33 / 291.63 s | 8 / 26 | Entry and four suites passed in another 0.77 s; [runtime retrocheck](</private/tmp/slc-performance-evidence/compile-k6mg7y/runtime-retrocheck.json>) failed repository ownership |
| [Opus 5, fresh phase sessions plus optimizer edit](</private/tmp/slc-performance-evidence/compile-AoStYE/summary.json>) | 330.64 s | 194.19 / — | 5 / 14 | Failed FSM after malformed optimizer output; session effect is confounded |
| [Opus 5, corrected optimizer examples](</private/tmp/slc-performance-evidence/compile-hmOuZA/summary.json>) | 300.16 s | 73.63 / 139.13 s | 5 / 13 | Coverage checker false positive; separate runtime check rejected an incorrect repository disposition |

| Additional settled experiment | Scope | Compile / link | Calls / tools | Result and limit |
| --- | --- | ---: | ---: | --- |
| [Opus 5 broader demo](</private/tmp/slc-performance-evidence/compile-fIne1M/summary.json>) | Full, 797-byte demo | 416.17 s | 6 / 8 | Failed FSM after three calls; malformed upstream GEARS remained unchanged |
| [Default linker, fixed FSM](</private/tmp/slc-performance-evidence/compile-bayvzK/summary.json>) | Link only | 262.66 s | 1 / 18 | Linked contract passed; full runtime acceptance not tested |
| [Materializer v1, same FSM](</private/tmp/slc-performance-evidence/compile-j8qjkm/summary.json>) | Link only | 204.91 s | 1 / 10 | Linked contract and retrospective strict TypeScript passed after an agent corrected helper arity; full runtime acceptance not tested |
| [Materializer v2, Opus 5](</private/tmp/slc-performance-evidence/compile-yoHZsO/summary.json>) | Link only | 7.50 s | 1 / 0 | Provider rate limit; no throughput result |
| [Materializer v2, GPT-6 minimal effort](</private/tmp/slc-performance-evidence/compile-VMjtQM/summary.json>) | Full attempted | 25.10 s | 1 / 0 | Provider rejected unsupported effort; no throughput result |
| [Materializer v2, GPT-6 low effort](</private/tmp/slc-performance-evidence/compile-1EMnzp/summary.json>) | Full attempted | 38.91 s | 1 / 2 | Normalized artifact produced, then rejected for an invalid empty clarification marker; no accepted phase or full output |
| [Materializer v2, conditional clarification](</private/tmp/slc-performance-evidence/compile-ebky3Y/summary.json>) | Full minimal | 753.64 s | 5 / 29 | Generation finished; strict checking rejected generated `SCHEMA_FINDINGS` declarations; separate runtime retrocheck also failed |
| [Full helper recipe, fixed FSM, GPT-6](</private/tmp/slc-performance-evidence/compile-kZMeS0/summary.json>) | Link only | 106.42 s | 1 / 11 | Failed on a genuine contradictory completion-mapper clause; invalid comparison baseline |
| [Compact recipe, same FSM, GPT-6](</private/tmp/slc-performance-evidence/compile-XFta7p/summary.json>) | Link only, canceled | 84.27 s | 1 / 2 | Canceled after the contract finding; elapsed cancellation time is not a throughput result |
| [Corrected full recipe, GPT-6](</private/tmp/slc-performance-evidence/compile-Yf2Gxp/summary.json>) | Link only | 152.59 s | 1 / 12 | Strict TypeScript and linked contract passed in another 1.03 s; separate real-Git runtime probe passed; handwritten scaffold |
| [Corrected compact recipe, GPT-6](</private/tmp/slc-performance-evidence/compile-dwrKfO/summary.json>) | Link only | 79.05 s | 1 / 5 | Linker could not locate definition helper assets; no accepted output or throughput comparison |
| [Full recipe with definition origin, GPT-6](</private/tmp/slc-performance-evidence/compile-B6ZwA7/summary.json>) | Link only | 70.30 s | 1 / 6 | Strict and linked checks passed in another 1.03 s; separate real-Git runtime probe passed |
| [Compact recipe with definition origin, GPT-6](</private/tmp/slc-performance-evidence/compile-VWL108/summary.json>) | Link only | 96.70 s | 1 / 8 | Strict and linked checks passed in another 1.05 s; separate real-Git runtime probe passed; compact technique rejected |
| [Matched full recipe without helper, GPT-6](</private/tmp/slc-performance-evidence/compile-8LOA2N/summary.json>) | Link only | 67.18 s | 1 / 9 | Failed the previously disproved host-boundary audit; no linked output |
| [Matched full recipe with helper, GPT-6](</private/tmp/slc-performance-evidence/compile-ukgJ3t/summary.json>) | Link only | 80.21 s | 1 / 12 | Strict and linked checks passed in another 1.02 s; separate real-Git runtime probe passed; no valid elapsed comparison |
| [Cold full helper recipe, GPT-6](</private/tmp/slc-performance-evidence/compile-aZda4G/summary.json>) | Full minimal | 407.14 s | 5 / 20 | Strict, entry and four suites/eight tests passed; corrected runtime replay accepts unchanged artifacts; original failed summary retained |
| [Common-guide baseline without helper, GPT-6](</private/tmp/slc-performance-evidence/compile-D6r6pK/summary.json>) | Link only | 128.81 s | 1 / 16 | Failed a false audit of additional runtime-owned Boss-text fields; actual installed classifier probe passes |
| [Cold full fresh-phase sessions, GPT-6](</private/tmp/slc-performance-evidence/compile-52Dess/summary.json>) | Full minimal | 352.11 s, failed | 5 / 20 | Link rejects real source-FSM TS2352 after 74.51 s; no accepted output; session technique not retained |
| [Quoted-relay fixed-FSM baseline, GPT-6](</private/tmp/slc-performance-evidence/compile-oNiH7r/summary.json>) | Link only | 213.00 s | 2 / 17 | Mechanical import correction followed by strict/linked success; separate runtime acceptance passed; valid matched baseline |
| [Quoted-relay fixed-FSM helper v3, GPT-6](</private/tmp/slc-performance-evidence/compile-YQHwJk/summary.json>) | Link only | 78.68 s | 1 / 9 | Strict/linked and separate runtime checks passed; matched 63.1% observed linking reduction |
| [Cold full v3 recipe, GPT-6](</private/tmp/slc-performance-evidence/compile-sxeXw2/summary.json>) | Full minimal | 371.27 s | 5 / 22 | First original full acceptance: strict, entry, four suites/eight tests and real runtime pass; 373.80 s total |
| [Cold full fast-mode request, GPT-6](</private/tmp/slc-performance-evidence/compile-bUlAXi/summary.json>) | Full minimal, failed | 273.42 s | 5 / 17 | Linker refuses pending-question context layout; no linked output or independent validation; no accepted fast-mode speed claim |
| [Original broader demo with strict gate, GPT-6](</private/tmp/slc-performance-evidence/compile-ACwgrV/summary.json>) | Full attempted, 797-byte demo | 62.18 s | 2 / 3 | Structured clarification at text2gears; source unchanged, no FSM/link calls; feature success, no compile acceptance |
| [Explicitly clarified broader variant, GPT-6](</private/tmp/slc-performance-evidence/compile-bs5rV2/summary.json>) | Full attempted, 1,540-byte variant | 900.02 s, deadline | 6 / 23 | FSM takes 571.42 s; deadline interrupts linking after 131.67 s; no linked output or independent acceptance |
| [Matched no-optimize minimal run, GPT-6](</private/tmp/slc-performance-evidence/compile-9wgFW3/summary.json>) | Full minimal, failed | 277.80 s | 4 / 13 | Optimization omitted; linker refuses pending-question layout after 54.19 s; no output acceptance or retained speed claim |
| [Retained broader FSM linked separately, GPT-6](</private/tmp/slc-performance-evidence/compile-6Rpcgc/summary.json>) | Link only | 139.09 s | 1 / 16 | Strict and linked checks pass in 1.19 s; original v6 compiler, unchanged prior FSM; separate three-scenario runtime replay passes, coverage unresolved |
| [Canonical-storage minimal run, Opus 5](</private/tmp/slc-performance-evidence/compile-kqEmCN/summary.json>) | Full minimal, failed acceptance | 174.17 s | 5 / 9 | All phases finish; strict entry checking rejects required `inputTask`; suites and runtime not run; no five-minute acceptance |

The early-gate row's historical summary says `success`: that harness revision did not yet include source-level runtime acceptance. It is **not** an accepted successful benchmark. Failed earlier runs never reached the separate emitted-suite validation stage.

**What consumes the time.** In the Opus baseline, linking accounts for 47.9% of compilation and FSM generation for 27.0%, together 75.0%. The linker receives 155,590 prompt bytes and makes nine tool calls; the FSM call receives 51,896 bytes. The early normalize, text-to-GEARS and optimize phases still consume 82.54 seconds. GPT-6 shifts more time into FSM generation: 45.9%, versus 22.9% in link. These observations prioritize generation, tool work, and avoidable correction; they do not establish a general model ranking.

First-event latency totals 31.48 seconds across the five baseline Opus calls, about 9.5% of compile wall time, with individual waits from 3.27 to 10.20 seconds. This combines SDK/process startup and provider/network waiting; the measurement cannot isolate those causes. It overlaps each call's duration. Summed call lifetimes can also exceed phase wall time because adapter cleanup overlaps boundaries, so these figures must not be added as independent costs.

Prompt bytes are **not tokens**. Opus reports 1,777,463 input tokens across tool turns, comprising 1,633,520 cache reads, 143,903 cache writes and 40 uncached tokens, plus 14,871 output tokens. These are provider accounting units that repeatedly include context; they are not 1.78 million unique source tokens or uncached input. The split run reports more total input despite a smaller initial prompt. GPT-6 token accounting is partial and absent from several calls; missing counts remain unknown. The JSON uses each call's top-level totals once, without adding duplicate per-model records.

**Correctness failures explain wasted work.** The baseline's bad FSM reached link and consumed another 158.22 seconds before rejection. A [deterministic replay](</private/tmp/slc-gpt6-engine-audit/early-gate-evidence.json>) detects that same prompt mismatch at the earlier seam in 46.36 milliseconds. The candidate recovered its FSM through one same-Coder correction, then needed two link corrections. Recovery is demonstrated; a validated net speed improvement is not.

The GPT-6 linker also stopped for an alleged engine defect at the wrong validation layer. An [actual installed Captain-host probe](</private/tmp/slc-gpt6-engine-audit/evidence.json>) rejected all three malformed-capability cases before constructing the runtime. The relevant installed and sibling validator slices are byte-identical. This establishes a false-positive audit failure, not permission to weaken the host contract.

Finally, optimization replaced an exact current-directory repository condition with an ancestor-repository check. The [exact baseline script probe](</private/tmp/slc-baseline-git-probe-72xgd94k/evidence.json>) exits zero while leaving the nested directory without its own `.git`. This probe is not an emitted-entry test. The later candidate's real runtime retrocheck confirms why generated suites alone are insufficient: mutually consistent artifacts can still change source behavior.

The corrected-optimizer run finishes compilation in 300.16 seconds, but still does not count as successful. Its coverage checker incorrectly rejects a declared script failure handled by an unguarded fallback; a retained real-XState probe demonstrates the actual success and failure routes. Separately, its linker declares a commit-producing action as `unchanged`, which the real repository-effect boundary rejects. Upstream `14a8ce6` makes the required commit disposition explicit even when the outcome has no effect-owned payload field. Coverage correction is committed as `d640d0f`: the current checker accepts the unchanged FSM in 26.01 ms, versus the former false finding in 25.76 ms. The [before](</private/tmp/slc-coverage-fallback-before.json>) and [after](</private/tmp/slc-coverage-fallback-after.json>) evidence retain identical FSM and linked-module hashes. This replay does not replace the historical emitted checker or turn the failed runtime result into acceptance.

The broader demo exposes another misplaced repair loop: malformed `Results` text originates in text2gears and survives optimization, but the FSM consumer spends 282.91 seconds across three calls trying to repair a target whose upstream source is protected. The [existing contract parser](</private/tmp/slc-gears-producer-evidence.json>) detects the original error in 0.81 ms and its optimized copy in 0.48 ms. Commit `a599b2f` adds producer validation and consumer preflight. An [ordinary-compiler replay](</private/tmp/slc-gears-consumer-preflight-evidence.json>) rejects the unchanged malformed source in 175.69 ms, with no executor selected and zero agent calls; pure parser work takes 0.082 ms. This demonstrates avoiding the three doomed consumer calls, while successful full-compilation performance remains unproved.

The fixed-FSM comparison is promising but provisional. Materializer v1 reduces observed linking time by 57.75 seconds, or 21.99%, and tool calls from 18 to 10. Both runs use the same 12,554-byte FSM and pass the linked contract. However, the default run overlaps the broader demo's provider work for about 118 seconds; the helper run has different concurrent load, and neither has full source-level Git runtime acceptance. The v1 helper also needed an agent to correct a 12.3-versus-13.1 API-arity difference. V2 supplies the correct arity mechanically, but its first Opus attempt hit a provider limit. These observations justify continuing the experiment, not adopting a production speed claim or subtracting link time from a different full run.

An [independent strict TypeScript audit](</private/tmp/slc-generated-typecheck-audit/evidence.json>) accepts `hmOuZA`, `k6mg7y`, `j8qjkm` and the corrected v2 helper fixture in 0.87–0.98 seconds each. It copies unchanged artifacts into a temporary ESM consumer, preserves installed dependency resolution, and uses locked TypeScript 6.0.3 with ES2022/NodeNext, strict and unused-symbol checks, erasable syntax, native `.ts` imports and `noEmit`. Four runtime suites alone would miss a typed API-arity error. Commit `d4e5933` adds this independent check to subsequent full and link-only benchmark acceptance, under the same deadline; it does not retroactively replace historical timings.

| Technique | Decision at this checkpoint |
| --- | --- |
| Bounded measurement, declared-input identities, strict TypeScript and source-level runtime acceptance | Retained as verification infrastructure (`abfef9b`, `d4e5933`); no speed claim |
| Script-result coverage fallback correction | Retained correctness fix `d640d0f`; current checker passes the unchanged FSM; no speed claim |
| Historical Playbook materializer v1/v2 trials | Supporting correctness evidence; manual correction and unequal concurrent load prevent an isolated speed claim. The explicit quoted-relay v3 row below is the current retention decision |
| GEARS producer validation and consumer preflight | Retained correctness fix `a599b2f`: real consumer preflight rejects malformed input in 175.69 ms with zero agent calls; full successful speed evidence pending |
| Canonical continuation-input producer and consumer gate | Retained correctness fix under DR-040; final replay rejects the unchanged defect in 591.59 ms with zero link calls; scope is input wiring, not full durable Q&A |
| Strict FSM producer and supplied-source checks | Retained correctness fix `289780e`: unchanged invalid FSM rejected in 575.89 ms with zero link calls, before the historical 74.51-second failed link; no successful net speed claim |
| Omit optimization with the existing CLI option | Matched candidate fails before output acceptance; one fewer call does not establish a successful speed improvement, and defaults stay unchanged |
| Explicit Codex fast-mode request | Candidate fails before linked output and validation; not retained as a demonstrated speed improvement; effective provider tier unobserved |
| Strict generated verification constants | Retained correctness fix `73b87dd`; regenerated suites pass strict checking with entry, FSM and linked module unchanged |
| Machine-root public identity gate | Retained correctness fixes `f8c757e`, `61c3300`; reject the entire reserved root namespace; earlier unchanged-FSM replay rejects before linking in 10.63 ms |
| Definition reference origin | Retained correctness fix `15b338d`; six real-filesystem cases and 120 relevant tests pass; no isolated speed claim |
| Compact link recipe with full-contract fallback | Rejected: matched accepted link-only runs take 70.30 s for full and 96.70 s for compact, with tools increasing from six to eight |
| Explicit quoted-relay materializer profile | Retained for matched flat quoted linking: 213.00 → 78.68 s (63.1% observed reduction), with all link/runtime checks passing; one pair, cold full target pending |
| Split the large link definition into referenced sections | Rejected: initial link prompt shrank 79.5%, but link time increased 23.2%, total time increased 7.9%, tools increased, and compilation still failed; upstream Playbook revert `de60d8e` |
| Isolated supported Codex SDK 0.153.4 | Retained as benchmark environment setup: removes the observed compatibility blocker while preserving the ordinary Cligent permission wrapper; no compiler speed claim |
| Early FSM conformance and bounded same-Coder repair | Recovery demonstrated; accepted performance evidence pending |
| Preserve exact repository-root predicates during optimization | Upstream fix `f0f032b`; follow-up `40d8538` corrects examples to actual GEARS blockquotes after the fenced-body failure; corrected live acceptance pending |
| Fresh session at each phase's first call | Not retained: earlier evidence was confounded; the later same-compiler/definition full candidate fails on a real FSM type error despite a shorter FSM phase |
| Typed FSM construction helpers | Unimplemented, unmeasured and deferred until link materialization is evaluated; estimated 15–25% emitted-byte reduction, with no latency claim |
| Noninteractive source clarification | Retained feature: live Opus check returned structured questions and exit 2 in 19.16 s, with unchanged source, no accepted output and no build history; not a successful compile |

The [clarification evidence](</private/tmp/slc-live-clarification-27abde0w/evidence.json>) remains local because its questions quote source text. Only result flags, identities and timing are copied into the sanitized record.

The later GPT-6 low-effort attempt writes a normalized artifact and reports successful agent work, then appends an empty clarification marker. The strict decoder correctly rejects that malformed protocol, so the run fails after 38.91 seconds and no phase is accepted. Its [private transcript replay](</private/tmp/slc-performance-evidence/compile-1EMnzp/clarification-replay.json>) identifies unconditional prompt wording as the trigger. Commit `684119a` makes the instruction conditional on unresolved source behavior; successful unambiguous work omits the marker, while the decoder remains strict. This is a clarification-prompt correction, not a model throughput result.

With that prompt correction, GPT-6 completes all five generation phases in 753.64 seconds, including 299.09 seconds for FSM generation and 307.81 seconds for linking. Independent strict checking then fails in 1.09 seconds because two deterministically generated suites declare an untyped empty `SCHEMA_FINDINGS` array. The original benchmark stops before entry loading, suite execution or runtime acceptance; generation completion is not full acceptance. A separate [runtime retrocheck](</private/tmp/slc-performance-evidence/compile-ebky3Y/runtime-retrocheck.json>) also fails. An [unchanged-FSM probe](</private/tmp/slc-deferred-contract-audit/root-metadata-evidence.json>) confirms that extra root-level `meta.playbook.stateId` metadata produces active ids `minimal` and `ready` together, removing the singular `stateId` expected by the shared runtime. The generated-suite typing defect and authored root metadata are separate problems.

Commit `73b87dd` fixes generated constant typing while preserving the assertions. A [strict-emitter replay](</private/tmp/slc-strict-emitter-probe/evidence.json>) fails before regeneration and passes afterward, changing only verification suites in a temporary copy; the original artifacts and copied entry, FSM and linked module remain unchanged. Commit `f8c757e` separately rejects root public identities through existing conformance. Its [real early-boundary replay](</private/tmp/slc-root-identity-probe/evidence.json>) checks the unchanged FSM in 2.83 ms and rejects it in 10.63 ms with zero link calls, before the historical 307.81-second link phase. Follow-up `61c3300` reserves the entire own root `meta.playbook` namespace, including empty and null values, while preserving the XState id and other metadata. Its 150 focused integration tests pass; the earlier timing probe measured the populated identity case. Neither correction retroactively accepts the failed full run.

The next fixed-FSM GPT-6 baseline stops after 106.42 seconds on a genuine definition contradiction. Unlike the earlier host-boundary false alarm, its exact full link definition allows only four completion-mapper members, excluding `deferred` and `unresolved` that the installed API and current canonical specification explicitly support. The [reproducible audit](</private/tmp/slc-deferred-contract-audit/evidence.json>) verifies the stale clause, three matching installed/tested host implementation hashes, and two passing real-Git deferred checkpoint tests. Synchronizing that clause with the existing specification resolves the documented discrepancy without changing the engine. The compact candidate is canceled after 84.27 seconds once this shared normative defect is established. These runs do not form an accepted timing comparison; a corrected, completed pair is still required.

The corrected full recipe subsequently passes fixed-FSM linking in 152.59 seconds, plus 1.03 seconds for independent strict TypeScript and linked-contract checks. Its one call uses 161,770 prompt bytes and 12 tools; the final module is a handwritten shared-factory scaffold, so this result does not establish that the optional materializer ran. The corrected compact candidate uses 17,876 prompt bytes and five tools, then fails after 79.05 seconds because the linker cannot locate the definition directory's helper and companion. Both assets appear in the recorded input closure; commit `15b338d` now supplies their definition origin through ordinary execution. These runs use identical settings, source, compiler, dependencies and helper bytes, differ only in `link.md`, and do not overlap. The failed candidate still prevents a throughput comparison, and the accepted baseline remains link-only evidence. A separate [real-Git runtime probe](</private/tmp/slc-fixed-link-runtime-probe/Yf2Gxp.json>) subsequently accepts the baseline in 823 ms, preserving its FSM and linked module and reusing an existing entry. That check is outside the original timing and does not supply a full cold compilation or four-suite result.

The definition-origin correction supplies JSON-quoted absolute definition file and directory paths to interpreted and compiled performing calls. Six integration cases read real sibling helpers and references outside the workspace, across compile/link and interpreted/player/direct-Captain execution; all 120 relevant tests pass. Exact definition bytes, configured options, working directory and routing/judge prompts are preserved. The combined root suite after `61c3300` and `15b338d` passes 1,250 tests with two skips across 69 files.

With that correction, the new matched pair completes fixed-FSM linking in **70.30 seconds for the full recipe and 96.70 seconds for compact**. Independent strict TypeScript and linked checks add 1.03 and 1.05 seconds respectively; original validated totals are 71.34 and 97.76 seconds. Separate real-Git runtime probes accept [full](</private/tmp/slc-fixed-link-runtime-probe/B6ZwA7.json>) in 654 ms and [compact](</private/tmp/slc-fixed-link-runtime-probe/VWL108.json>) in 710 ms: each creates its own repository, preserves the exact Boss task, makes one delegated call and one commit, and reaches success. Inputs and linked artifacts remain unchanged, and each probe reuses the preserved entry.

The pair uses identical compiler, settings, source, dependencies, harness and helper bytes, differs only in `link.md`, and has no provider overlap. Both outputs carry the materializer generation marker and scaffold. Compact cuts initial prompt bytes from 162,178 to 18,290 (88.72%), but linking takes 26.40 seconds longer (37.55%) and tools rise from six to eight. **The compact recipe is rejected for lack of demonstrated benefit.** One pair does not establish a universal latency ranking. The prior 152.59-second full-recipe run used a different compiler and handwritten scaffold, so its difference from the new result does not isolate the origin correction. All these accepted results remain link-only; the cold full-compilation target remains open.

A further matched pair changes only the optional helper recipe in the full link definition, with identical source, compiler, settings, dependencies, harness and other declared input bytes, and no provider overlap. The baseline stops after 67.18 seconds without output, repeating the already disproved shared-factory versus Captain-host audit from `EXAwtt`; this is separate from the earlier completion-mapper contradiction. The helper candidate passes linking in 80.21 seconds and strict/linked validation in another 1.02 seconds, followed by a separate [real-Git acceptance probe](</private/tmp/slc-fixed-link-runtime-probe/ukgJ3t.json>) in 698 ms. This is an outcome contrast, with **no valid elapsed ratio** between failed and accepted compilation. Its 5,083-byte linked module is byte-identical to the accepted `B6ZwA7` artifact (SHA-256 `fb7778110a8c07ffa9fdc1a6e671865165bc1f047b61cb7667d80fb953c881e8`), consistent with deterministic helper emission; it does not independently prove tool invocation or full-compilation throughput.

The next cold full run completes generation in 407.14 seconds, including 179.79 seconds for FSM generation and 118.59 seconds for linking. Strict TypeScript, entry import, and all four generated suites (eight tests) pass. Its original runtime check then rejects the first performing prompt because it searches for the raw multiline Boss task, while the linked composer quotes every complete line with `> `. The original summary remains failed at 413.31 seconds total. A corrected [real-Git replay](</private/tmp/slc-quoted-task-acceptance-probe/evidence.json>) accepts those unchanged artifacts in 819 ms, recording exact quoted task text, its own repository, one delegated call, one commit and a successful terminal. Original summary, diagnostics and compiled-artifact hashes remain unchanged. This establishes retrospective acceptance of the cold-generated workflow; the separate replay is not added to the historical timing. Generation alone exceeds five minutes, so the target remains unmet.

Commit `86d8b76` follows [DR-038](../specs/decisions/038-quoted-task-runtime-acceptance.md): accept either the unchanged literal task or an exact contiguous block of whole lines prefixed with `> `, preserving every other character. All 32 measurement integration tests pass, including 12 independently rerun real-runtime cases and rejection of changed, missing, reordered and inconsistently quoted lines. All repository and execution checks remain binding; this is a validation correction with no compiler speed claim.

The next no-helper baseline includes the same host-boundary guidance in the common introduction, then fails after 128.81 seconds on a different false audit: it treats the standard `BOSS_INTERRUPT.bossIntent` field and the FSM's additional `bossInputTask` field as incompatible. An [actual installed-classifier probe](</private/tmp/slc-boss-interrupt-contract-probe/evidence.json>) shows both fields receive exactly the original Boss text when the additional field is declared `source: text`; forged Judge text for either field is rejected before work begins. The accepted classification updates the FSM context, clears pending question/reply state and completes one delegated call and commit. The probe uses the unchanged FSM and captured accepted-link metadata, omitting only deterministic entry metadata to exercise classification from `ready`; all source and copied file bytes remain unchanged. The linker's refusal produces no artifact and no accepted timing comparison.

Upstream commit `600fa8c79` on `codex/link-materializer-quoted-relays` adds the explicit `flat-quoted-relays` candidate. It preserves literal substitution, whole-line quoting, LF/CRLF and blank lines, missing values and installed continuation behavior, while leaving the default profile's output byte-identical to archived v2. Nineteen real-CLI tests pass against each actual Playbook 12.3 and 13.1 engine. Over the unchanged `aZda4G` FSM, its [isolated emission](</private/tmp/playbook-materializer-v3-aZ-probe/emission-evidence.json>) produces a 5,859-byte linked module that passes strict checking, link fidelity and all four unchanged suites (eight tests); the [real-Git runtime probe](</private/tmp/playbook-materializer-v3-aZ-probe/runtime-evidence.json>) passes in 787 ms. The original source, entry and suite files retain their hashes. This isolated emission establishes correctness; the subsequently completed matched linking comparison below supplies the scoped performance evidence.

A bounded [inspection of the exact FSM tool transcript](</Users/basicthinker/.codex/sessions/2026/09/12/rollout-2026-09-12T12-26-07-01a09715-9035-75d1-b941-458e50013628.jsonl:102>) identifies one possible avoidable detour. The agent selects global TypeScript 5.0.2 instead of workspace 6.0.3; its first check spends 1.75 seconds failing on dependency declarations, followed by 18.87 seconds before a successful retry with `skipLibCheck`. Supplying a deterministic command using the workspace compiler and existing strict settings is an unimplemented hypothesis, with **no demonstrated saving**. Most of the FSM phase still precedes emission: 97.88 seconds elapse between the source-read result and the request to write the 11,137-byte FSM. That interval does not isolate model computation from reasoning, rendering, network or scheduling. The 179.79-second GPT-6 FSM phase and earlier 73.63-second Opus 5 phase use different intermediate GEARS and definition revisions, so they are not a controlled comparison.

The new fresh-session full run uses the same compiled JavaScript, source, declared pipeline bytes, model, effort and dependency versions as `aZda4G`, with the corrected runtime-acceptance harness. All five calls actually start without inherited continuation. FSM generation takes 147.80 seconds rather than 179.79, but linking spends another 74.51 seconds before rejecting the real source error `output: {} as void` (TS2352), leaving no accepted target. Compilation fails after 352.11 seconds before independent artifact validation. The phase reduction does not demonstrate an accepted speedup, so **fresh phase sessions are not retained**.

The quoted-profile comparison now has an accepted [fixed-FSM baseline](</private/tmp/slc-performance-evidence/compile-oNiH7r/summary.json>) over the unchanged `aZda4G` FSM. Linking takes 213.00 seconds across **two calls and 17 tools**: the second call mechanically corrects a `.js` import to the actual `.ts` FSM. Independent strict TypeScript and linked checks add 1.05 seconds, for 214.05 seconds total. A separate [runtime replay](</private/tmp/slc-quoted-fixed-link-runtime-probe/oNiH7r.json>) passes in 718 ms using the preserved aZ entry, with exact quoted text, its own repository, one delegated call and one commit; original and copied hashes remain unchanged. The matching v3 helper completes in 78.68 seconds, plus 1.05 seconds for independent validation (79.73 seconds total), using one call and nine tools. The [matching task's completed tool invocation](</private/tmp/playbook-materializer-v3-pair-audit/helper-invocation.json>) confirms that the phase actually executed the quoted-relay materializer successfully and emitted 5,886 bytes. Its separate [runtime replay](</private/tmp/slc-quoted-fixed-link-runtime-probe/YQHwJk.json>) passes in 684 ms with the same quoted-task and repository requirements. Source, compiler, settings, link target, harness and all declared input member bytes match except `link.md`; the runs are sequential without overlap. The observed 134.32-second reduction (63.1%) supports retaining this flat quoted-relay technique, including avoidance of the baseline's import correction. One pair does not isolate pure emission cost or provide a general latency estimate, and neither run proves the cold full-compilation target.

The matched quoted-link result is independently reproducible: the [task-scoped helper audit](</private/tmp/playbook-materializer-v3-pair-audit/evidence.json>) confirms actual successful invocation and re-emits a byte-identical 5,886-byte module from its exact descriptor. Upstream retention commit `1c1ba298e` records the same limited flat quoted-link scope and preserves a sanitized evidence snapshot.

The subsequent [cold full run](</private/tmp/slc-performance-evidence/compile-sxeXw2/summary.json>) is the first to pass **all original acceptance checks**: generation takes 371.27 seconds and strict checking, entry import, four suites/eight tests and real runtime acceptance add 2.52 seconds, for 373.80 seconds total. It uses five calls and 22 tools; FSM generation takes 172.65 seconds and linking 94.33 seconds. The 797 ms runtime check creates its own repository, relays the exact quoted task, performs one delegated call and one commit, and reaches success. The same 292-byte source and frozen v5 compiler use the v3 helper recipe; no separate task-command audit is asserted for this full run. This establishes original full acceptance, while the five-minute target and an isolated full-compilation speed ratio remain open.

The [fast-mode request](</private/tmp/slc-performance-evidence/compile-bUlAXi/summary.json>) changes only `SLC_FAST_MODE=true` against that accepted cold run, preserving source, frozen compiler, declared inputs, settings and harness, without provider overlap. All five calls record the request, but the effective provider service tier is unobserved. It fails after 273.42 seconds, including 156.94 seconds for FSM generation and 39.73 seconds for linking, with five calls and 17 tools. The linker refuses a source that nests its pending question under `context.continuation`, alleging the installed runtime expects the flat context field. An [actual-runtime audit](</private/tmp/slc-boss-continuation-audit/evidence.json>) confirms that nested-only layout is an authored defect. There is no linked output or separate acceptance, so this failed duration supplies **no accepted fast-mode speed comparison**. Saved configuration remains unchanged.

Commit `289780e` adds the [strict FSM boundary](../specs/decisions/039-strict-fsm-boundary.md), using SLC-owned TypeScript and Node declarations while preserving artifact import resolution. Generated FSM type findings enter the existing two-repair budget; supplied invalid FSMs stop before consumer construction. The [unchanged-artifact gate replay](</private/tmp/slc-fsm-strict-probe/gate-evidence.json>) rejects `52Dess` in 575.89 ms with zero link calls, avoiding its historical 74.51-second doomed link, and accepts valid aZ in 772.26 ms before a stubbed consumer. Real timer cancellation stops before consumer construction after yielding pending events. The full suite passes 1,270 tests with two skips, including 15 strict-boundary cases, and the [production-only packed installation](</private/tmp/slc-fsm-strict-probe/packed-final/evidence.json>) accepts valid and rejects invalid standalone artifacts. These are correctness and avoided-failure results, not a successful full-compilation speed ratio.

The [original 797-byte broader demo](</private/tmp/slc-performance-evidence/compile-ACwgrV/summary.json>) then stops after 62.18 seconds with `compile.outcome: clarification-required` in text2gears. Two questions identify the missing decision after an unresolved third judgment and the meaning and terminal behavior of the two-loop cap. Both the original and copied source retain their hashes; only normalization is accepted, and no FSM or linker call starts. This validates clarification on an actual broader workflow, rather than silently choosing domain behavior. The benchmark records failure and exits 1 for non-success; it does not measure the ordinary CLI’s separately tested clarification exit 2. Those choices are now explicit in the separately identified [1,540-byte benchmark variant](performance/clarified-workflow.txt).

The separate [clarified benchmark source](performance/clarified-workflow.txt) explicitly fixes those choices while leaving the original demo untouched; its 1,540 bytes match the recorded private variant. The [broader run](</private/tmp/slc-performance-evidence/compile-bs5rV2/summary.json>) reaches its 900-second deadline during linking: FSM generation alone takes 571.42 seconds, followed by 131.67 seconds of incomplete link work. Six calls and 23 tools include one optimization correction. No linked output or independent acceptance is produced. A separate [private reference control](</private/tmp/slc-broader-runtime-probe/clarified-positive-evidence.json>) validates clean, agreement and capped-failure runtime scenarios, including the final findings; it is not generated-run acceptance. Unrecognized phase granularity or untyped terminal presentation remains an unsupported probe profile.

Commit `5f81da1` adds the [continuation-input gate](../specs/decisions/040-early-continuation-input-contract.md), which reuses the existing canonical question/reply sentinel check. Its [final unchanged-artifact replay](</private/tmp/slc-boss-continuation-audit/gate-evidence.json>) rejects bUl in 591.59 ms with zero link calls, before the historical 39.73-second failed link; valid aZ reaches a stub consumer in 688.32 ms. All 1,288 tests pass with two skips. Independent review caught and verified the fix for import-time source mutation: preflight now checks the same declared protected inputs and rejects changes before selecting a consumer. This input check does not prove durable Q&A behavior; a separate [disk-checkpoint and real-commit probe](</private/tmp/slc-sxeXw2-deferred-probe/evidence.json>) passes over unchanged accepted sxeXw2 artifacts.

A [separate link run](</private/tmp/slc-performance-evidence/compile-6Rpcgc/summary.json>) links the unchanged 41,891-byte broader FSM in 139.09 seconds, plus 1.19 seconds for strict and linked checks, using one call and 16 tools. It uses the actual frozen v6 compiler and does not replace the failed full-run history or establish cold compilation. A [separate reconstruction and replay](</private/tmp/slc-broader-complete-replay/replay-evidence.json>) preserves the original artifacts and generated assertions, passes strict TypeScript and the GEARS/FSM, introspection and prompt suites, and passes three actual Playbook 12.3 host scenarios: clean uses two delegated calls and one commit; agreement uses six and two; capped failure uses 15 and three, plus one Captain report preserving the exact last review findings. These source-specific scenarios do not prove every path or Boss-reply behavior.

Coverage remains failing. Commit `6ba16d0` preserves supplied top-level event fields during candidate evaluation and read tracing: the unchanged machine actually follows `ready`→`failed`→`ready`, contradicting two earlier checker findings. The narrow fix changes total findings from 44 to 39—seven disappear and two become visible—with no broader reflective-field inference or waived findings. This completes the bounded broader evaluation with explicit limits; it is neither full accepted compilation nor a five-minute result.

Upstream commit `c8a8c47` makes the existing scalar/keyed Boss-question storage paths explicit in the [FSM producer](</private/tmp/playbook-link-materializer/slc/gears2fsm.md>), following actual reader failures on privately nested-only state. Six real XState/shared-reader matrix cases pass. The frozen overlay changes only `gears2fsm.md`; the retained [v3 helper candidate](</private/tmp/playbook-link-materializer/slc/materialize-link.mjs>) and other declared inputs retain their bytes. This is a correctness clarification with no isolated speed claim, and no installed release changes.

The existing `--no-optimize` option is now [measured against the accepted minimal baseline](</private/tmp/slc-performance-evidence/compile-9wgFW3/summary.json>) using the same v5 compiler, source, settings, declared inputs and harness, with only optimization disabled and no provider overlap. Four calls and 13 tools take 277.80 seconds before failure: FSM generation takes 162.75 seconds and linking 54.19 seconds, then refuses another nested-only pending-question layout without writing the target. No independent acceptance runs, so omitting the pass is **not retained as a demonstrated speed technique**. This comparison intentionally predates the new gates and does not confound their compiler version with phase removal.

The [next Opus 5 low-effort run](</private/tmp/slc-performance-evidence/compile-kqEmCN/summary.json>) completes all five generation phases in 174.17 seconds: normalization 26.71, text2gears 14.94, optimization 14.73, FSM generation 71.15 and linking 46.53 seconds. Five calls use nine tools. Final strict checking then rejects the generated entry because its linked runtime requires `inputTask`; total time is 175.25 seconds, and entry, suites and runtime acceptance do not run. The frozen v8 compiler and canonical-storage producer differ from earlier full trials. This raw failed outcome supplies no five-minute success or isolated speed claim; the options-contract diagnosis is separate. A [separate actual-runtime audit](</private/tmp/slc-kq-entry-audit/evidence.json>) proves two defects in unchanged artifacts: a required constructor copy of future Boss-turn text, and a missing typed actor-input field that leaves the task placeholder unresolved even when that constructor value is supplied. The FSM’s entry event already copies the exact Boss text. The correction belongs at its producer contract; broadening generic entry options or casting away the type error would not restore the missing relay.

**The shell's current environment is different.** A [read-only resolution probe](</private/tmp/slc-global-runtime-audit/evidence.json>) found that `/opt/homebrew/bin/slc` loads global SLC **0.7.0**, its nested Playbook **12.2.0**, Cligent **0.24.0**, XState **5.32.6**, and globally supplied Claude SDK **0.3.220** / Codex SDK **0.146.0**. The separate global Playbook **12.0.0** is not the dependency this SLC executable resolves. The benchmarks above use repository SLC **0.9.0**, Playbook **12.3.0**, XState **5.32.4**, and the SDK versions recorded per run.

From `/Users/basicthinker/Projects/SubLang/playbook`, ordinary `slc playbook` discovers the home config: **Claude Code, `claude-opus-4-8`, high effort, no Reviewer**, with a 600-second inactivity watchdog. No relevant selection environment overrides were observed. Its installed definitions measure 20,078 bytes for text2gears, 48,990 for gears2fsm, 152,671 for link, and 4,468 for optimize. Both installations therefore carry large FSM/link definitions, but their hashes differ; neither the model/effort nor compiler behavior matches the measured baseline. These differences can affect latency and correction behavior, but no global-versus-repository speed ratio has been measured. The older global compiler also lacks the new clarification module.

Working directory matters: the SLC repository's local config replaces home discovery and selects its vendored `pipelines/playbook`, while leaving model and effort to provider defaults. Running the global binary from that directory still uses the global compiler and its default Playbook link-target dependency. A repository checkout alone does not upgrade the shell command.

After final validation, the immediate reproducible path is an explicit invocation of the validated build with per-run model/effort overrides, rather than relying on `PATH` or changing saved defaults:

```sh
SLC_AGENT=claude-code SLC_MODEL=claude-opus-5 SLC_EFFORT=low \
  node /Users/basicthinker/Projects/SubLang/slc/dist/cli.js playbook <source>
```

A permanent upgrade must install the final validated SLC release or packed artifact and its required SDK versions, then repeat the CLI-anchored resolution probe to confirm the actual dependency tree. Upgrading only the separate global Playbook cannot replace SLC's nested copy. No global installation or saved configuration was changed during this audit.

The packaging checkpoint after `5f81da1` passes the SLC 0.9.0 package smoke check (177 packed files), both English and Chinese reference demos, all three artifact checks, definitions, release checks and pins. The demo signing override is process-local. The latest root checkpoint, `6ba16d0`, passes 1,290 tests with two skips, including 78 coverage tests, plus build, changed-file lint/format and Spex checks. These checks support packaging and regression correctness, not compilation throughput.

**Reproduction and limits.** Install the locked dependencies, build, then run the current benchmark:

```sh
npm ci
npm run build
node scripts/benchmark-compile.mjs --agent claude-code --model claude-opus-5 --effort low --timeout-seconds 1200 --label verified-opus5-low
```

Every run creates a fresh evidence directory. The deadline covers compilation and validation. The default minimal workflow now requires independent strict TypeScript checking, entry import, all four generated suite kinds, and a real installed-host-capability runtime check: its nested directory must acquire its own repository before exactly one synthetic performing agent receives the exact Boss task and makes one commit, then reaches a successful terminal. An arbitrary `--source` receives this specific acceptance check only with `--runtime-check minimal`.

The JSON records reconstructed historical commands, including the frozen compiler and split pipeline paths. For GPT-6, the preserved isolated tree at `/private/tmp/slc-gpt6-runtime-mlgyv8pk` was installed with ordinary offline `npm ci` followed by `npm install --offline --save-dev --save-exact @openai/codex-sdk@0.153.4`; no global configuration or root lockfile was changed. See its [runtime setup evidence](</private/tmp/slc-gpt6-runtime-mlgyv8pk/runtime-evidence.json>).

The frozen baseline and isolated compiler JavaScript directory digests match: `0b6d85a4a01fdab7a78d32699a398f7874e7ab5b6b0069252b41db1940e125aa`. They were hashed retrospectively from preserved copies. The baseline and isolated lock hashes differ and are recorded in full in the JSON. Exact executing-runtime hashes were not captured for the earlier live clarification and mutable early-gate candidate; those identities are explicitly unavailable. New measurements capture SDK versions and compiled JavaScript identities automatically. Commit `abfef9b` also captures the executing benchmark/runtime-check scripts and the complete **declared** semantic-input closure through the measured compiler’s boundary checks, including `slc.pin-inputs.json`, helper scripts and applicable transitive references. Older summaries retain their original root-file-only records; their missing closure/harness identities remain unavailable. Aggregate closure and harness hashes include absolute paths, so compare member byte hashes when roots differ. The v1 and v2 closure digests are `450d73821a660d3879161417a5c51bfaea0b6abc050034b89506f3b0b8fb298c` and `477b5a3a13150474359883eebdb0c87fa0dfb221e59ae91102b658491ebe0401`; full member identities are in the JSON.

The unsupported GPT-6 `minimal` effort and Opus rate limit are settings/provider failures, not measurements of model throughput. Single observations and changing provider conditions do not prove causal speedups. The remaining performance target is a correct full compilation under five minutes. The bounded broader-workflow evaluation is complete with the acceptance limits above; its unresolved coverage findings are not waived. Ongoing runs are excluded from this settled-results checkpoint.
