<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: 2026 SubLang International <https://sublang.ai> -->

# Complex workflow evaluation

Evaluation is in progress. One accepted matched DECIDE phase pair shows a 31.1-second (24.0%) reduction with Results-boundary guidance, which is now retained. The C7-v9 full DEV run `compile-wm3i4U` is accepted: the full benchmark completed in 858.323 seconds overall, generated-suite validation passed eight tests, and a separate post-benchmark runtime control passed all 24 DEV scenarios. The C7-v8 full CODE run `compile-GQcJFN` still has a failed full benchmark because exact coverage retained three redundant REVIEW fallback findings, even though a separate runtime control later passed all 18 CODE scenarios. The fixed-FSM DEV link candidate `compile-43wE00` succeeded with the actual materializer in 168.355 seconds overall and its post-measurement staged entry, generated suites, and 24 DEV runtime scenarios passed, but the paired baseline failed at the 600-second boundary, so no percentage speed-ratio claim is accepted. A full DEV mutant now delivers the intended clarification in a later phase. The [reviewed design](clarification-corpus-design.md) predates these runs; the [follow-up record](performance/complex-workflows-followup-2026-09-12.json) preserves exact outcomes and evidence.

## Initial current-dependency cohort

The immutable C1 compiler uses production commit `84b80bb`, the `39475e7` measurement harness, Playbook 13.2.0, Cligent 0.27.0, Claude SDK 0.3.270, and XState 5.33.0. All runs use one Opus 5 agent, low effort, fast mode off, no Reviewer, and actually selected compiled `text2gears`. [Dependency adoption](dependency-adoption.md) records the remaining compatible TypeScript and Node-type constraints.

| Family | Clean execution | Restored execution | Injected source defect | Independent outcome |
| --- | ---: | ---: | --- | --- |
| CODE | 143.9 s | 100.2 s | Review required and forbidden after every owned commit | Targeted question in 17.9 s. Restored translation passes; clean review-predicate placement remains inconclusive until runtime validation. |
| DECIDE | 119.4 s | 102.2 s | Concurrent proposals versus waiting for Coder | Targeted question in 23.5 s; both translations pass. |
| REVIEW | 101.9 s | 55.7 s | Return versus another round under the same completion condition | Targeted question in 16.9 s. Clean translation passes; restored translation omits required revision relays from all four actor prompts. |
| DEV | 129.2 s | 109.3 s | Reachable documentation-only outcome without an action | Intended omission identified in 53.3 s, but incomplete JSON causes an execution error. Both sufficient translations pass. |
| Support triage | 59.0 s | 64.3 s | Two destinations for the same urgent ticket | Targeted question in 15.4 s; both translations preserve routing and runtime customer questions. |

Times are recorder execution durations, including ordinary phase checks and excluding separately recorded preparation. These are phase measurements, not full compilations. Clean/restored timing differences are uncontrolled model variation, not optimization savings. The [complete initial matrix](performance/complex-workflows-2026-09-12.json) retains every attempt, exact source/cohort identities, call counts, preservation/history evidence, and private independent adjudication hashes.

No sufficient-source invocation asked a compiler question. Of ten positive invocations over five unique sources, eight pass semantic review, one is inconclusive, and one loses authored behavior. Four of five mutants deliver valid questions about their intended defect; the fifth recognizes the defect but fails report serialization. Every source and recorded cohort remains unchanged, and no phase probe publishes successful build history. These observations demonstrate the cases, not a universal accuracy rate.

## Findings and follow-up controls

The REVIEW loss is a compiler-output defect: the original source explicitly requires revision information in each acting prompt. Existing transformation rules already require this delivery. A narrow common clarification now states that conditions, result contracts, and machine context do not substitute for the required prompt relay. The C2 rerun restores the revision relay in all four actor prompts in both sufficient-source outputs. The clean case passes semantic review in 111.7 seconds, and the contradictory case produces the intended question in 18.4 seconds. The restored case completes in 159.5 seconds but separately drops the obligation to return the evaluated revision and absence of unsettled findings. That sufficient-source output remains rejected despite passing mechanical checks.

With the common terminal-return correction, C3's restored REVIEW now explicitly returns both required facts. It still fails compilation after 119.4 seconds because sixteen generated relay lines add labels the Source never authored; the strict source gate allows only bare quoted placeholders for such prose-required inputs. This is a compiler formatting defect, not unresolved Source behavior. The matching contradiction still produces one targeted question in 25.6 seconds. Formatting guidance is being aligned with the existing gate before repeating the positive case. None of these API probes establishes a CLI exit code or full runtime acceptance.

DEV's visible model final already lacked the top-level closing brace. The strict decoder correctly rejected it. Commit `2832d63` reinforces complete JSON syntax in the shared performing prompt, with no parser relaxation, automatic answer invention, new model call, or interactive step. Frozen C2 changes only that production prompt and its generated source maps; all C1 dependency and pipeline bytes remain unchanged. The C2 DEV rerun took 158.3 seconds and completed the first phase without a question, preserving the reachable `documentationOnly` result but supplying no route for it. This misses the targeted first-phase oracle; later detection remains untested. Since it emitted no report, the rerun does not establish report-guidance effectiveness. The actual CLI source-only repair check passes: CODE's contradiction produces one targeted diagnostic, empty stdout, exit `2`, and no accepted output or history in 21.0 seconds; changing only the source back to the exact original and repeating identical arguments produces exit `0`, no question, and a semantically accepted GEARS translation in 106.5 seconds. All 11,888 recorded compiler/dependency members remain unchanged.

The direct GEARS triplet also passes its tested scope through actual interpreted `gears2fsm`: the sufficient controls compile in 156.1 and 121.9 seconds without questions, while the contradictory terminal outcome produces one targeted question in 31.8 seconds. Each generated control passes six real XState execution cases for approval, rejection, runtime question/resume, exact field relays, and malformed actor outputs. These are generated-FSM checks with deterministic actor outcomes, separate from linked runtime acceptance.

A separate definition conflict incorrectly equated child bridge completion with all caller-required success conditions. The Playbook branch now distinguishes bridge/terminal handling from explicit caller-authored acceptance predicates. The runtime is unchanged; real Git integration checks verify that CODE rejects missing or ill-typed review evidence and unsettled findings after bridge delivery.

Repeated mechanical Results repairs are a measured cost. In three first-cohort runs, moving prose below Results caused another formatting error and another correction; those second correction dispatch intervals total 75.075 seconds. The accepted sequential DECIDE pair isolates 379 bytes of Results-boundary guidance: execution falls from 129.649 to 98.559 seconds, calls from three to two, and tool calls from seven to four. Both final translations pass independent source review. Both still needed an initial correction; only the baseline needed a second, Results-only correction. The observed saving is 31.090 seconds (24.0%) in one baseline-first phase pair, not a randomized estimate or full-compilation saving. The exact measured guidance is retained in upstream commit `d353c8f` and included identically in subsequent common definitions.

The fixed-CODE link pair uses the same 36,725-byte maintained FSM, C2 compiler, dependency graph, and Opus 5 low settings; only the optional materializer recipe differs. Both original recorder runs pass strict TypeScript and the canonical link gate. Their broader acceptance differs:

| Run | Generation | Calls / tools | Independent source-aware acceptance |
| --- | ---: | ---: | --- |
| A7u5ik, baseline | 309.772 s | 1 / 24 | Rejected: multiline relay substitution drops quote prefixes. The unchanged maintained suite reports 23/30 passing. |
| RyJuic, materializer recipe | 133.602 s | 1 / 12 | Accepted for fixed CODE and the tested parent-runtime scope: all 29 applicable assertions pass. The original 30-assertion suite remains failed on an inapplicable exact context-projection expectation. |

RyJuic’s additional effect-derived `latestCommit` progress field is permitted by the frozen contract, which requires an explicit safe projection rather than the maintained implementation’s exact field list. No artifact or assertion was changed to reach this separate adjudication. Its literal relays, continuation, own commit, nested REVIEW, failure handling, options, and receipt checks pass with scripted child outcomes. The baseline’s proven defect prevents a ratio between two accepted link builds; neither run measures natural-language compilation or actual child implementations. Separately, the already measured Git-observation optimization reduces paired real repository observation/receipt overhead by roughly 116–153 ms; that is host overhead, not model or full workflow latency.

The failed full CODE attempt VucDX9 took 640.1 seconds, stopped at the FSM gate, and produced no linked artifact. Its raw and optimized GEARS are byte-identical and pass independent source review. The old gate incorrectly expected raw nested prompt text where the current contract requires composed child text. Separately, the generated FSM invented REVIEW output fields and rejected the actual public success result; the isolated compiler inputs lacked those public interfaces. These are distinct compiler and environment defects, not choices for the source author. The historical failed run remains unchanged. One private diagnostic file was later corrected from citation `playbook-22` to `playbook-26`; reversing that single citation change reproduces its original recorded hash exactly. The follow-up record preserves both identities and the unchanged artifact hash.

## Corrected C3 cohort and next comparisons

Frozen C3 uses production and harness commit `b091e80`. Relative to C2, only the composed-child fidelity checker and private validation-cache handling change; dependencies, bundled pipeline files, and pins remain byte-identical. Both actual execution-strategy probes pass. The four active v4 definition closures separately share the public workflow interface catalog and literal target bindings, retained Results guidance, and terminal-return correction. The catalog supplies public interfaces without maintained CODE/DEV/DECIDE/REVIEW implementations.

Upstream commit `671fe37` freezes two independent comparison pairs. The v4 link pair differs only in `playbook/link.md`. The fixed-GEARS scaffold pair differs only in `playbook/gears2fsm.md`; the optional helper and guide are present identically in both arms. The scaffold preserves literal constants and supplies an incomplete typed action skeleton, leaving routing and acceptance predicates to the compiler agent. It remains unmeasured. Fourteen real C3 assembly/discovery tests pass, the compiler inventory and existing cache remain unchanged, and independent readiness confirms the same Playbook/XState runtime graph. These checks establish input readiness, not generated-artifact acceptance.

All optimization comparisons require accepted matching artifacts and source-specific validation. Full CODE/DEV latency and correctness remain separate from phase and fixed-FSM measurements; failed attempts are retained.

## Later-phase clarification and full CODE diagnosis

The original DEV omission mutant now passes its end-to-end clarification oracle. Run SQkdsA accepts the intermediate GEARS, then stops in `gears2fsm` after 190.888 seconds with one complete question about the reachable documentation-only outcome. The source defines neither its action nor its terminal meaning. The report identifies the original source to edit, gives distinct possible source changes, and selects none. No FSM or entry is emitted, and all 11,903 recorded inputs remain unchanged. This satisfies the first-or-later-phase requirement; it establishes observed report delivery, not a causal estimate for the JSON wording or a universal detection rate. The benchmark wrapper reports failure because no runnable artifact was produced; the actual API outcome is `clarification-required`.

The second full CODE attempt, CKr2Ni, generates all four phases in 762.093 seconds but fails strict entry validation. Its accepted GEARS preserves the source, and its canonical REVIEW success predicates pass 40 pure guard checks. Remaining problems are distinct:

| Finding | Evidence and correction |
| --- | --- |
| First-turn Boss text became required startup configuration; the deterministic entry also cannot express legitimate structured or required options. | Source guidance distinguishes actual pre-turn requirements from Boss-entry text. Current entries will use the linked artifact's own pure option validator, with a strict factory-derived type and the existing bounded repair path. |
| Handwritten child-failure validation disagrees with the host's public union. | Four host-valid failure envelopes are rejected. Upstream commit `889f497` reuses the public validator; its focused actual-bridge cases pass. The structural reduction is not a measured provider-latency saving. |
| Ten child-coverage findings are false. | Actual XState paths enter and settle all ten unchanged arms. Commit `6633268` follows real predecessor transitions and uses reached context; deeper and non-preemptive coverage is being extended separately. |

No linked runtime is accepted by bypassing the invalid public entry. Prepared CODE runtime cases and pure guard checks remain separate from full acceptance. The larger workflow's measured phase costs are 142.457 seconds for text translation, 14.979 for a byte-identical optimization result, 405.712 for FSM generation, and 193.521 for linking. This makes FSM authoring and linking the next performance targets; the unchanged optimization output alone does not establish a safe general skip.

## REVIEW representation controls

The v5 common guidance aligns bare relay formatting and Results placement. Three fresh C3 phase observations preserve the full repeated-run record:

| Run | Time / calls | Independent outcome |
| --- | ---: | --- |
| V4nTvl, exact restored REVIEW | 100.682 s / 2 | Translation accepted: all four required relays and explicit terminal return obligations are present. |
| 1y1wBw, contradictory REVIEW | 15.822 s / 1 | One valid targeted clarification; no GEARS artifact or history. |
| AegKvB, exact original REVIEW | 107.786 s / 3 | Rejected: seven added tokens have only the outer GEARS marker, leaving unquoted prompt content. No source clarification is warranted. |

The two sufficient cases contain identical source bytes; their different outcomes demonstrate remaining generation variation. Commit `e11033b` keeps the same strict rejection but now gives the exact `> > <token>` GEARS example needed to preserve `> <token>` in a prompt. Upstream quote-layer wording makes the same distinction. Runtime delivery of AegKvB's receipt-owned revision relay still requires downstream proof; its field name alone is not a semantic rejection.

The literal relay correction passed 1,355 integration/system tests with two expected skips. The subsequent bounded child-chain correction passes 1,381 tests with two skips across 73 files. Its actual maintained DEV coverage takes 11.237 seconds and now exercises the non-preemptive actor and child paths previously skipped; this is checker readiness, not acceptance of a newly compiled DEV workflow. Build, formatting, ESLint, Spex 3.0.0, definition/release/artifact/pin checks, both demo languages and the installed-package smoke pass (181 packed files); test-process Git signing was disabled and the temporary package install used network access. Historical C1–C4 compilers and v3–v6 definitions remain unchanged.

Prepared C4/C5 and v6/v7 inputs extend those checks without accepting a new performance or full-workflow result. C4 uses production commit `c021339` from a private archive build: relative to immutable C3 it changes 37 `dist/` files, while dependencies, pipelines, pins, and scripts remain unchanged. C5 uses production commit `662d73a`; relative to C4 it changes only three `verify-coverage` dist files, with dependencies and pipeline inputs unchanged. The 2,400-second stall watchdog default from external commit `8db05c6` is included among the C4 input differences and carries no optimization claim. The v6 link pair is frozen under upstream commit `642875f` with boundary proof evidence; the v7 pair is frozen under upstream commits `fe68dd3` and `dac4d33`, differs from v6 only by the IR-085 authored-question correction, and selects no scaffold guidance. The scaffold-v3 fixed-GEARS readiness confirms the same 8,432-byte VucDX9 GEARS source, identical current initializer and guide files across both arms, and only `playbook/gears2fsm.md` differing.

The settled scaffold-v3 pair produces no accepted speed ratio. The baseline was interrupted at 600.809 seconds; its corrected retrospective C3 checks supersede the original wrong-API audit and pass GEARS/FSM conformance plus continuation on the deadline FSM, without changing the interrupted verdict. The treatment was also interrupted at 600.810 seconds after one successful call and one interrupted continuation; its deadline FSM typechecks and passes continuation, but still has two GEARS/FSM conformance findings for literal substitution and quoted continuation preservation. The optional scaffold guidance has not earned retention from this pair and is not selected for the next full run; the prototype and evidence remain historical.

C4-v6 full CODE compile `sTXoB6` was coordinator-cancelled, not accepted or timed out. The wrapper was interrupted with SIGINT at PID `1248` after repeated raw/optimized GEARS omission of `question: <verbatim final text>`; cancellation landed in link after text translation, optimization, and FSM generation had produced accepted phase artifacts. The run records 589.044 seconds compile time overall, with phase times 114.906 seconds, 11.276 seconds, 459.464 seconds, and 3.345 seconds into the cancelled link. Its 8,479-byte raw/optimized GEARS share SHA-256 `7961ba623023e00f7043802652edd6648241c769e316600e5c0aeabe89d30414`. This observed omission is distinct from the known CKr2Ni actual reconciler/bridge question-field failure proof, and it adds no runtime acceptance or speed claim.

C4-v7 CODE source phase `Qe8qAK` succeeded in 90.771 seconds of execution time and 91.538 seconds total, across two calls of 63.771 and 27.609 seconds. The original case-001 source remains the same 5,687-byte input, SHA-256 `42987d1a13ee38e0f555c32dec6bb0d32c31153c5d855e56f8fc7ea97333ade5`. Unlike the old CODE output, generated `workflow.gears.md` line 42 explicitly declares `question: <verbatim final text>`; C4 result-contract and source-fidelity checks pass, and no framework `needsBossReply` result was emitted. This is source-phase evidence only, with no runtime acceptance or causal speed claim.

The CKr2Ni authored-question audit proves one additional generated-artifact defect without repairing or accepting the old artifact. The source-authored question outcome requires final question text, but the generated FSM/result authority omitted the declared `question` field. The actual reconciler and bridge therefore delivered only the guard and drove the unchanged FSM to failure; a presentation-owned authority-data positive control adding `question` reaches the expected wait state. The broken public entry was not bypassed. The existing guard search can synthesize missing fields, so this diagnosis does not justify an unsound generalized guard-read requiredness gate.

## C5 full-workflow failures and early checks

The next two unchanged-source runs remain failures, recorded in [the September 13 evidence](performance/complex-workflows-2026-09-13.json). CODE `anx8n4` returns compiler success in 730.886 seconds, but the complete benchmark fails in 747.623 seconds: two unreachable child-review fallback arms fail generated coverage, and actual public runtime execution cannot return a suspended REVIEW child. The child leaves carry both busy and suspended tags. A private copy changing only those two tags completes the direct CODE path through the same public entry and installed host in 728.950 milliseconds, with one real commit and exact prompt relays. That diagnostic proves the suspension defect; it does not accept the original artifact, erase the coverage findings, or adjudicate authored questions.

DEV `Kdavz2` fails after 572.980 seconds of compiler execution. Its generated DEV-5 clause inserts `first` between `shall` and `call playbook`; the current parser classifies it as direct Captain work, and the FSM agent rejects that malformed actor clause without writing its target. The original workflow is sufficiently clear about ordering. This generated syntax defect calls for producer repair rather than clarification from the source author.

CODE spends 134.962 seconds in text translation, 14.978 in optimization, 341.453 in FSM generation, and 224.974 in linking. DEV spends 147.257, 14.590, and 411.049 seconds before stopping at FSM generation. Early rejection can avoid downstream work on these known defects, but these failed observations establish no accepted full-workflow speed ratio.

C6 and v8 preparation establishes input identity and selection readiness independently of benchmark acceptance. C6 freezes exact source commit `f44d4c5d751a2b4fbc21b279b5c3cc4a3cf82e2c` under `/private/tmp/slc-c6-compiler-ukczlhl3`, excludes uncommitted IR054 harness edits, changes seven `dist/` files from C5, and archives committed helper scripts including the existing release and verification scripts; dependencies, pipelines, pins, package manifest, and lockfile remain byte-identical. The C6 wrapper readiness is `/private/tmp/slc-c6-wrapper-readiness/evidence.json` with SHA-256 `5f13f83d359706f1bbd15b9e004fc65bbb2e308e4fe592892354116622da5ba2`. The v8 baseline/full inputs are selection-ready under `/private/tmp/playbook-13.2-common-v8-{baseline,full}` with readiness SHA-256 `7a9e79aa28611d2ab2dd95377c06158db84e20443d3d5c2c2f634ea4f2509f85`; relative to v7, v8 adds only definition corrections for child-busy handling and canonical call/onDone wording, with no scaffold selected.

The C6-v8 minimal run `compile-5eWTLQ` is the first accepted result in this September 13 set, but its scope is only the original 292-byte minimal source. It completed compilation in 237.479 seconds and the full benchmark in 243.459 seconds across six provider calls; strict TypeScript, entry import, and four generated suites with eight tests passed. The actual minimal runtime check used the literal Boss task, made one own-repository Git commit, and finished in 1.224 seconds. The invocation proof records all 11,920 protected inputs unchanged before and after. This confirms the current minimal path in under five minutes; it does not establish a causal speed attribution across compiler or definition versions and does not accept either full CODE/DEV workflow.

The v9 baseline/full inputs are prepared-only under `/private/tmp/playbook-13.2-common-v9-{baseline,full}` with readiness SHA-256 `bfe705884ef3c2c821c5e848b1040c65d6faad2fe8e8ac266c54665bc68ca616`. Relative to v8, v9 changes only the common `gears2fsm.md` producer to add the IR091 source-state preservation guidance; the inverse proof recovers v8 producer `4e840accb47c1924be6e63a483f928a592d36edb07c57be82dbecff59b022ce9`, and the baseline/full arms still differ only in `playbook/link.md`. The supplied C7 compiler records the private IR090 runtime overlay in its cohort evidence, so this is not an unmodified published Playbook runtime dependency. This preparation adds no provider, benchmark, generated-suite, runtime acceptance, or performance claim.

The C6-v8 full DEV run `compile-uh9JIL` remains a failure. It compiled for 850.582 seconds and the wrapper finished after 850.597 seconds: text translation took 132.120 seconds, optimization 12.692, FSM generation 383.741, and linking 321.942 before the link phase failed after the third checking round while analyzing multiline quoted-relay prompt composition. The last accepted artifact is the generated FSM at `/private/tmp/slc-c6-complex-full-evidence/compile-uh9JIL/work/dev.playbook/dev.fsm.ts` with SHA-256 `2f079ddb92e140a761411e0f0eb84217f7228a32d4405f7c2cb1d27b72b05a3d`; no emitted suite or runtime acceptance ran, and no source clarification was reported. Summary `dce23127156bda22fdae9d087f57e226b431a2da49679a1cfb4184d3c807d34e` and invocation proof `c964bd6b1c720fbf42dc0559ea6621525891a152c952036b12f246e66e5cfd4f` record all 11,920 protected inputs unchanged before and after. A read-only probe of the unchanged final composer reproduces CRLF separator loss: it splits relayed values on CRLF and rejoins them with LF. The failing probe has no blank lines and retains its quote markers. This diagnostic does not accept or repair the artifact.

A separate unchanged-factory DEV runtime audit finds two source-behavior defects before another provider run. First-turn `discussionComplete` reaches success despite the required prior Boss reply. After two fresh question continuations, the third Analyst prompt retains the current exchange but loses an earlier explicit CLI-only/UI-preservation constraint; its archived discussion is empty. Canonical CODE success and BRANCH failure controls preserve exact child results and stopping behavior. The audit preserves all 11,660 C6 dependency entries and establishes no full-compilation acceptance. Its adjudication corrects only derived question-presence booleans from a consumed fixture label array, using the unchanged recorded prompts and responses.

C7 preparation is recorded as prepared-only input identity for the next live runs, not as a measured result. It freezes SLC commit `c7419edcf1f609bc9bcb9074b9414586f9d7be03` under `/private/tmp/slc-c7-compiler-y4iln8vx`, with cohort evidence `/private/tmp/slc-c7-compiler-y4iln8vx-evidence/cohort.json` SHA-256 `519aca78c24093b75eb525cb45e5e820d98b077cc0ee5809cf20b5ef2ef4ced2` and preparation evidence SHA-256 `b1712b3ff75cd257d87c25926cdd1fab6cdf8145b9aee5526e0172164bcc5c06`. Relative to C6 it rebuilds committed IR055 source and scripts, regenerates private pins SHA-256 `df0f75a45ac9f723d562e2bb0c3f25a03cdaea3d634692cfa5f4c13744511779`, and keeps pipeline artifacts, package manifest, and lockfile unchanged. The only dependency changes are the documented private IR090 Playbook runtime overlay: exactly `xstate-playbook-runtime.ts` and `.js` from upstream `4b51a7edfd2670f6a4cb4f23285a897ae836e612`, overlay patch SHA-256 `91a1059e34ec59495d0f435a4200602512b891bb651376f2145434e40d3e92a9`. The original C7 wrappers are preserved as historical because the first CODE launch failed at JavaScript parse time before provider or proof execution; the replacement v2 wrappers pass `node --check`, with full wrapper `/private/tmp/slc-c7-complex-full-run-v2.mjs` SHA-256 `49f27ca07d66dfd421738e9817d5f7ecb2eb27ef817ac3cdd74cbec7b9ba0fab`, minimal `44a77bc0ea30cf7a2c6ce7a97f3bbd24a3fc1fd94d42a41e2262dea0f2fd32d0`, and link `dba11421489d406948e64e226f03545f5ee41d48b8ac515a8119be9e83e70346`. Wrapper adjudication `/private/tmp/slc-c7-wrapper-v2-adjudication.json` SHA-256 `fe8a175267748cdcdad3234c27d97c34aff7d226696a565e09fefd6167d7b3c8` supersedes only the faulty derived metadata in the historical v2-readiness file; no compiler, cohort, manifest, inventory, or benchmark artifact was regenerated for that correction.
C7-v8 full CODE run `compile-GQcJFN` compiled successfully in 738.641 seconds, but the full benchmark failed after 742.618 seconds during generated-suite validation. The phase times were 83.229 seconds for text translation, 12.813 for optimization, 466.102 across FSM generation, and 175.820 for linking over five provider calls. Strict TypeScript, entry import, prompt contract, GEARS/FSM conformance, and introspection passed; the generated suite reported seven passing tests and one failed coverage test, retaining exactly three findings: `reviewDirectPhase` nested `onDone` arm 2 unsatisfiable, `reviewNewIrPhase` nested `onDone` arm 2 unsatisfiable, and `reviewIrTaskPhase` nested `onDone` arm 3 as an unsupported repeated-invocation path. A read-only actual XState audit at `/private/tmp/slc-GQcJFN-coverage-audit/evidence.json` SHA-256 `c646a74022daabdf3a783e12196d5801a930b137c0429faf805c86cb85824582` covers 12 cases, reproduces the checker findings in 500 milliseconds, observes all six real review entries assigning a nonblank commit, and concludes all three final `onDone` fallback arms are redundant rather than missing Source behavior. The audit kept all 24 artifact files and 11,660 dependency entries unchanged. The run used the actual flat-labelled materializer, records all 11,920 protected inputs unchanged before and after, and asked no source clarification. The original full benchmark remains failed; this is not an accepted speed-ratio or runtime result.

A separate post-benchmark runtime acceptance control for C7-v8 CODE `compile-GQcJFN` passes all 18 scripted CODE scenarios through the actual emitted public entry `work/code.ts` and C7 host APIs, with zero provider calls. Its summary `/private/tmp/slc-c7-code-GQcJFN-runtime-acceptance-v2/summary.json` has SHA-256 `6ed6b4549707b6538e65d9c665cffa484741e998e39297f7d1150da899d52252`; manifest `/private/tmp/slc-c7-code-GQcJFN-profile-draft/runtime-acceptance-v2-manifest.json` has SHA-256 `38273592426d3f82640bdd7e90dd439073d8000422eae5e749b151791c5a2b42`. The profile maps only GQcJFN's advertised guards and public output fields, uses the source-authored `question` outcome with repository disposition `unchanged`, and drives scripted REVIEW children with the artifact's accepted `{ noUnsettledFindings: true, evaluatedRevision }` child output. All protected GQcJFN source, GEARS, FSM, linked factory, public entry, and C7 runtime files stayed unchanged; the direct dependency inventory check covered 11,660 entries and 656,995,360 bytes with zero changes, evidence SHA-256 `ec9bc2b03c398235291eb534d9650f12af0422114d103a7c0f3f3c38929b3165`. This runtime control does not change the original full benchmark verdict: `compile-GQcJFN` remains a failed full benchmark because its emitted coverage suite retained the three redundant REVIEW fallback findings.

A C8 pre-provider gate probe confirms those same GQcJFN coverage findings now stop a real supplied-FSM link before model work. Frozen C8 commit `46a89ced91ea565cab0f41862f39257885219c93` is staged at `/private/tmp/slc-c8-compiler-ezn78503` with cohort SHA-256 `cc1dbd88f3c167e98ad587d466ebf2a57139c05fa7f18b3f351d509fea4babdf` and inherits the private IR090 Playbook runtime overlay rather than an unmodified published 13.2 runtime. Running C8's ordinary `runSlc` boundary on the unchanged C7 GQcJFN FSM with v9-full definitions rejects in 1.555 seconds before any adapter request or executor selection, writes no target, produces no clarification, and reports the same three coverage findings. Evidence `/private/tmp/slc-c8-gqcjfn-earlycoverage-cC17Q1/evidence.json` has SHA-256 `54449e8e80159e7073ed3e23efdd2a7f3ecb226c05353584cec73b80218ee935`; compact before/after tree identities record the C8 compiler and C7 CODE artifact unchanged. This establishes avoided provider work on a real invalid FSM, not a full-compilation speed ratio or acceptance of the failed GQcJFN artifact.

The C7-v9 full DEV run `compile-wm3i4U` completed successfully in 858.323 seconds overall, with compilation returning ok after 851.017 seconds. Text translation took 141.590 seconds across two calls, optimization 12.585, FSM generation 315.551, and linking 375.917 over five total provider calls. Strict TypeScript, entry import, and all four generated suites with eight tests passed; summary `/private/tmp/slc-c7-complex-full-evidence/compile-wm3i4U/summary.json` has SHA-256 `964bed784c2d8d616fa19efe1c14554f7e08fd136c1ac494c913fb8a18444415`. The settled public entry is `/private/tmp/slc-c7-complex-full-evidence/compile-wm3i4U/work/dev.ts` with SHA-256 `e2bb29864125150b6971945c56cb542f1f302baa9bf1f441778040402f98d2a5`; the linked factory is `/private/tmp/slc-c7-complex-full-evidence/compile-wm3i4U/work/dev.playbook/dev.playbook.ts` with SHA-256 `00813c5cae6ec2ce431d3680c953f82e3ab711f2cbc13f0eef3669007f5e95c0`. This artifact uses the ordinary `slc/link.md` FSM-to-runtime linker shared-factory output. The pre-runtime readiness file `/private/tmp/slc-c7-dev-wm3i4U-readiness.json` SHA-256 `41878b5a298fe47e4d5e79012c2671f400ae5a59f7f994917d7ad0f6d5750544` records 24 current harness cases and passing config-only smoke before the approved runtime control below.

A post-benchmark runtime acceptance control for accepted C7-v9 DEV `compile-wm3i4U` passes all 24 scripted DEV scenarios through the actual emitted public entry `work/dev.ts` and C7 host APIs, with zero provider calls. Its summary `/private/tmp/slc-c7-dev-wm3i4U-runtime-acceptance/summary.json` has SHA-256 `82ff7c803b8272d0a2227400d1b2aa6b7edbd6396d1444ebcef2d346ea2472ff`; runtime direct check `/private/tmp/slc-c7-dev-wm3i4U-runtime-direct-check.json` has SHA-256 `cb36c78444ad8d8873f48cdd7048a9f6df7c7693f88e8fb86007fcb3a52e5f41`. The profile maps only wm3i4U's advertised guards and public output fields, including `planningResult` for implementation routes and guard-only selection for the presentation-owned `needsBossReply.question`; it drives scripted BRANCH, DECIDE, CODE, and PR children with artifact-matching child outputs. All protected wm3i4U source, GEARS, FSM, linked factory, public entry, readiness files, summary, and log stayed unchanged after the run. The full C7 dependency inventory replay checked 11,660 records and 656,995,360 bytes with zero changes, evidence SHA-256 `eb3de1fe262981e6c1b02df11c7f24e597d5a072112fc98f4bbb4ca3d1fcafb8`.

The DEV fixed-FSM materializer-selection experiment is recorded as design evidence at `/private/tmp/slc-dev-materializer-selection-design.json` with SHA-256 `f1380dd8eb6db69b35d18a907e54ef4606255844914c0038a4c119d1405691d2`. Its baseline run `compile-GOLgqj` uses `/private/tmp/playbook-13.2-common-v9-baseline` to link the accepted wm3i4U FSM against the C7 Playbook runtime target, and failed at the 600 second boundary: compilation elapsed 600.516 seconds and the wrapper elapsed 600.524 seconds. Summary `/private/tmp/slc-c7-link-evidence/compile-GOLgqj/summary.json` has SHA-256 `307a46c506c7b30a97ecbbee99412b5f1e947bbdbf97d714fda243c0ae3952d4`; proof `/private/tmp/slc-c7-link-evidence/invocation-k1PJqy/proof.json` has SHA-256 `61a24d47db29476f961bc93b7f027fa3820d0418de63db17d72bb534831a5ab5` and records all 11,921 protected inputs unchanged before and after. The candidate run `compile-43wE00` session 7215 uses `/private/tmp/playbook-13.2-common-v9-prefer-materializer`, selected the actual materializer, and completed the link-only scope successfully: compilation elapsed 167.175 seconds and the wrapper elapsed 168.355 seconds. Summary `/private/tmp/slc-c7-link-evidence/compile-43wE00/summary.json` has SHA-256 `b7d27fe043575fc5d06641326c0f68f180a17a972a62aef76629acc9276cca73`; proof `/private/tmp/slc-c7-link-evidence/invocation-x5BsDb/proof.json` has SHA-256 `b80665095601d457a016f5c4d61d03b95855b6a17ce02abe91907f77874f87a0` and records all 11,921 protected inputs unchanged before and after. A separate post-measurement validation stage `/private/tmp/slc-c7-dev-43wE00-validation-stage` preserved the candidate FSM and linked module bytes, copied the original wm3i4U source and GEARS as outside-provider oracles, emitted the normal C7 public entry plus four generated suites, passed strict typecheck and entry import, and passed four suite files with eight tests; readiness SHA-256 is `ea85e16308c95f455c848b5974a6818d76484b58d197c0497acacb3695b60712`. After root review, a private runtime control drove 24 DEV scenarios through the staged public entry and C7 host APIs with zero provider calls, passing all 24 and leaving the seven staged/original artifacts unchanged; summary `/private/tmp/slc-c7-dev-43wE00-runtime-acceptance/summary.json` has SHA-256 `21d68b40c77ed84837373c18d5c1aa0f55fe0f2b273d44591428d77f2a9a7690`, and postrun evidence `/private/tmp/slc-c7-dev-43wE00-runtime-postrun.json` has SHA-256 `8c869aff7916395e1f8c5a30181ce4ef64ed0905d6f63fce73b62026fe4eb20c`. These validation and runtime controls are excluded from the measured link time. Because the baseline failed, this record makes no percentage speed-ratio claim.
