<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: 2026 SubLang International <https://sublang.ai> -->

# Compilation verification status

This completion audit separates accepted evidence from retained failures. The final CODE correctness evidence is accepted for the C10/v13 resumed phase chain from accepted GEARS; it is not a cold full-source CODE timing. Exact attempts and identities remain in the [current evidence](performance/complex-workflows-2026-09-13.json), [earlier follow-ups](performance/complex-workflows-followup-2026-09-12.json), and [performance analysis](compilation-performance.md).

| Requested outcome | Verified result | Scope/limits |
| --- | --- | --- |
| Update SLC dependencies | Current reviewed dependencies adopted; build, full suite, package smoke, artifact retention and pins passed. [Versions and exceptions](dependency-adoption.md); [current reproduction](compilation-reproduction-current.md). | None for the recorded dependency checkpoint. |
| Compile a simple demo in about five minutes | Current-dependency C6 `compile-5eWTLQ`: 237.479 s compilation, 243.459 s total, eight generated tests and actual Git runtime acceptance. | No general latency guarantee is claimed. |
| Explain and reduce slow compilation | FSM/link generation dominates; avoidable producer defects trigger expensive repairs in protected downstream inputs. Matched accepted savings: 23.98% for DECIDE text-to-GEARS, 63.1% for flat quoted-relay linking, and 67.6% for the accepted C10/v13 CODE link pair from the same accepted FSM. | No cold full CODE latency reduction is claimed. |
| Verify complex DEV | Unchanged current DEV `compile-wm3i4U`: 858.323 s total; eight generated tests and 24 runtime cases passed. | No accepted DEV speed ratio; the C8 DEV link pair remains raw observation because its baseline failed a public option contract. |
| Verify complex CODE | C10/v13 resumed from accepted C10 GEARS, produced accepted FSM `phase-2E2YFN`, then both matched link arms passed strict/import/eight generated tests, metadata/options controls, and 18 runtime cases. Baseline `crYwRj` took 638.219 s total / 636.735 s compile; private preference `aMUkFr` took 206.865 s / 205.227 s. | Accepted scope is a resumed phase chain, not cold full-source CODE timing. Prior C8/C9/C10 failures remain retained failures. |
| Avoid unnecessary compiler questions | None in the ten initial clean/restored phase runs; successful later CODE/DEV compilation phases preserve authored runtime questions. Later REVIEW rerun satisfies all four source oracles. | Original malformed or incomplete artifacts remain failures, even where no question occurred. |
| Detect intentionally flawed sources | Five reviewed mutation families identify their targeted ambiguity; clean/restored/later GEARS controls and the actual CLI contradiction exit `2` then source-only correction exit `0` are retained. | Integrated with the final C10/v13 phase-chain evidence; original malformed or incomplete artifacts remain failures. |
| Support ordinary compiler UX | API `clarification-required`; CLI one structured stderr report, empty stdout, exit 2. Actual CODE source-only correction and identical command then exit 0. Authored runtime questions remain distinct. | None for the verified protocol. |
| Retain only supported optimization claims | Compact prompting, scaffold forcing and omission of optimization are not retained as speed techniques. The exact tested materializer preference is retained in Playbook commit `69ed4ab`, with the CODE performance claim limited to its accepted pair. | No generalized or cold full-workflow speed claim. |
| Commit and resume autonomously | SLC and Playbook changes are committed on authorized branches; the completion audit has enough accepted evidence for root to commit docs/spec closure and disable the follow-up. | Root will replace this row after committing and disabling the automation. |

The accepted C10/v13 phase chain satisfies the final CODE correctness requirement for this audit through the verified chain from original source to unchanged accepted GEARS, real FSM `phase-2E2YFN`, real linked entries `crYwRj` and `aMUkFr`, normal entry emission, and 18 runtime cases per arm. It does not measure cold source-to-entry CODE latency.

The CODE and DEV runtime harnesses use real public entries, real isolated Git repositories and host/runtime code, with controlled player responses and scripted children. Their execution times measure host behavior, not live model latency. Required reviews, quoted payloads, repository receipts, malformed child results, and resumed Boss discussions remain acceptance conditions.

C7–C10 inherit an explicitly private Playbook runtime correction on top of package metadata 13.2.0. The improved producer definitions also live on the authorized Playbook worktree branch. These results do not assert that a published package or the globally installed `slc` command already contains those upstream changes. The historical global-command audit found SLC 0.7.0 with Opus 4.8 at high effort; measured runs explicitly select the frozen branch compiler and one Opus 5 agent at low effort.

Current compiler checks: 1,430 tests passed with two expected skips, plus native build, scoped lint, spec lint, definition verification, retained-artifact checks and current pins. The C9 failure, the first private fixture mistake, the corrected retrospective control, and the sandbox authentication failure are retained separately. C10/v13 closes the corrected CODE phase-chain path with accepted link/runtime evidence, while explicitly not counting as a cold full-source CODE timing.
